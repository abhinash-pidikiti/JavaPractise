package oopsconcepts;

public class MainMethod {
	
	
	public static void main(String[] args) {

	/*
	 * What is JVM? // a baseline program that provides the runtime configuration
	 * 
	 * What is main method? // entry point
	 * 
	 * what is main method declared as static? // JVM can directly call it directly 
	 * without creating the object
	 * 
	 * what is string[] args? // array of strings which can be passed as arguments.
	 * 
	 * what is NoSuchMethodError:main? //when a program is executed JVM first it searches
	 *  for public static void main if it does not find it will show this error.
	 * 
	 */
	}
}
